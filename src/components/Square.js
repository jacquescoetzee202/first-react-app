const divStyle = {
    backgroundColor: 'red',
    height: 200,
    width: 200,
    marginTop: 20,
    marginBottom: 20
}

const Square = () => (
    <div style={divStyle} className="container"></div>
);

export default Square;